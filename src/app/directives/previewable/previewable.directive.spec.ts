import { PreviewableDirective } from './previewable.directive';

describe('PreviewableDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewableDirective();
    expect(directive).toBeTruthy();
  });
});
